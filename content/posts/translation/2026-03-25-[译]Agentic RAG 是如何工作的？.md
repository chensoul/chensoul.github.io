---
title: "[译]Agentic RAG 是如何工作的？"
date: 2026-03-25 14:30:00+08:00
slug: how-agentic-rag-works
categories: [ "translation" ]
tags: [agentic-rag, rag, ai, llm]
description: "翻译 ByteByteGo 的《How Agentic RAG Works?》，并补充 AI 摘要、关键词拆解、适用场景、工程启发与落地建议，帮助更深入理解 Agentic RAG。"
---

原文：<https://blog.bytebytego.com/p/how-agentic-rag-works>

标准 RAG 系统的主要问题，其实不在检索，也不在生成。

问题在于，在生成发生之前，中间没有任何机制去判断这次检索是否真的已经足够好了。

标准 RAG 是一条单向流动的流水线：从查询，到检索，到响应，没有检查点，也没有第二次机会。

这对于那些答案简单明确的问题来说，通常运行得很好。

然而，一旦查询变得模糊，或者答案分散在多个文档中，或者第一次检索回来的内容看起来不错但其实并不对，RAG 就开始失去价值。

Agentic RAG 试图修复这个问题。

它基于一个简单的问题：**如果系统能够在回答之前先停下来思考，会怎么样？**

在这篇文章中，我们将看看 Agentic RAG 是如何工作的，它如何改进标准 RAG，以及在使用它时应该考虑哪些权衡。

![](/images/how-agentic-rag-works/01.png)

## 一次查询，一次检索

要理解 Agentic RAG 修复了什么，我们需要先明确标准 RAG 是如何工作的，以及它在哪些地方做得不够好。

一个标准的 RAG 流水线有一个很直接的流程：

- 用户提出一个问题。
- 系统把这个问题转换成一种数值表示，称为 embedding，它捕捉的是查询的语义含义。
- 然后系统搜索向量数据库——一种专门用于查找语义相似内容的数据库——并检索出最匹配的文本片段。
- 这些片段会连同原始问题一起传给大语言模型，然后由 LLM 基于检索到的上下文生成答案。

见下图：

![](/images/how-agentic-rag-works/02.png)

下图展示了 embedding 通常看起来是什么样子：

![](/images/how-agentic-rag-works/03.png)

对于那些直接、明确，并且面对的是一个组织良好的知识库的问题，这种方式效果非常好。

比如像“我们的退货政策是什么？”这样的问题。只要文档语料足够干净，几乎每次都能得到一个可靠的答案。

下面是一个典型的查询流示意：

![](/images/how-agentic-rag-works/04.png)

问题会在查询变得更复杂时出现。下面是几个场景：

- **模糊查询**：当用户问“我该如何处理税务？”时，他们可能指的是个人所得税、企业税务，或者非营利组织的免税身份。标准 RAG 无法澄清，也无法改写。它会原样接收查询，检索相似度得分最高的内容，然后寄希望于结果正确。
- **证据分散**：有时答案分布在多个文档中。一个员工问“外包人员的远程办公政策是什么？”时，就需要同时从远程办公政策和外包协议中获取信息。标准 RAG 通常只会从一个统一的 chunk 池中检索，而且并没有“如果第一个来源不够，就去检查第二个来源”的概念。
- **虚假的自信**：检索返回了一些看上去相关的内容，但实际上并没有回答问题。也许它确实属于正确的主题，但来自一份过期版本的文档。系统没有机制去区分“相关”和“实际上正确”这两者。无论如何，它都会生成一个自信的回答。

这三种失败模式有着相同的根本原因。

系统不会反思它检索回来的内容。

它不会问自己：这些结果真的已经足够好了吗？

![](/images/how-agentic-rag-works/05.png)

## 从流水线到控制循环

Agentic RAG 通过引入 AI agent 的能力，把这条线性流水线替换成了一个循环。

从本质上看，AI agent 是一种软件系统，它能够感知环境、做出决策，并采取行动，以某种程度的自主性去实现特定目标。

这里“agent（代理）”这个词很关键。

正如旅行代理会代表我们去寻找航班、协商交易一样，AI agent 会代表用户或系统完成任务，而不需要在每一个细小步骤上都持续获得指导。

在 Agentic RAG 中，流程不再是“先检索、再生成”，而变成了：检索、评估返回结果、决定是回答还是再试一次，如果有需要，就换一种方式重新检索。

见下图：

![](/images/how-agentic-rag-works/06.png)

“Agentic”这个词听起来可能像是在做营销包装，但在这里，agent 的含义是：一个 LLM 被赋予了做决策和调用工具的能力。

可以把它理解为：这个 LLM 不只是生成文本，它还能够选择采取行动，比如运行一次搜索、查询数据库、调用 API，或者决定在作答之前它还需要更多信息。

## 查询优化、路由与自我纠正

这让系统具备了标准 RAG 所没有的三种能力。

- **工具使用与路由（Tool use and routing）**：agent 可以根据问题决定应该查询哪个知识源。财务问题可能去 SQL 数据库；政策问题可能去文档存储；产品问题可能两边都要查。换句话说，agentic 系统会选择正确的地方，或者搜索多个地方。
- **查询优化（Query refinement）**：在搜索之前，agent 可以把一个模糊的查询改写得更具体。在搜索之后，如果结果看起来不够强，它还可以重新组织问题并再次尝试。agent 会在检索前后都对查询本身采取行动。
- **自我评估（Self-evaluation）**：当结果返回之后，agent 会检查这些结果，例如问自己“这些内容相关吗？”“完整吗？”“是否和其他信息冲突？”如果其中任何一个答案是否定的，agent 就可以换一个查询、换一个来源，或者两者都换。这直接解决了“一次性命中”的问题。

下图展示了 Agentic RAG 的高层方式：

![](/images/how-agentic-rag-works/07.png)

不过，把 Agentic RAG 理解为一个二元开关是有误导性的。

在最简单的形式里，它可以只是一个路由器，决定在两到三个知识库中查询哪一个。对于多数据源环境来说，这就已经比标准 RAG 有了实质性的提升。

再往前一步，你会得到像 ReAct（Reasoning + Acting，推理 + 行动）这样的系统框架：agent 在每一步之间交替进行推理和行动，在多个检索步骤之间不断评估。

见下图：

![](/images/how-agentic-rag-works/08.png)

再往更复杂的方向发展，就是多 agent 系统：多个专门化的 agent 在一个编排器的协调下协同工作。

“控制循环”是一个很有用的心智模型。不过，如果把它重新映射回前面提到的失败模式，会更容易理解。

- **用查询优化解决模糊性**：对于“我该如何处理税务？”这个问题，它会先经过 agent。agent 可以根据上下文把它拆解成子问题，或者先把它改写成更有针对性的表达，再进行检索。如果第一次检索回来的结果是关于个人所得税，而上下文表明用户其实在问企业税务，agent 就可以改写并再次搜索。
- **用路由解决证据分散**：关于外包人员远程办公政策的问题，现在会先经过一个 agent。这个 agent 会识别出它需要两个来源。它会先路由到 HR 政策文档库，检索结果；再路由到外包协议文档库，从那里检索；然后在生成答案之前，把两组检索结果综合起来。
- **用自我评估解决虚假的自信**：agent 检索到了一个看似相关的片段，但它来自一份两年前更新的文档。评估步骤会把这个问题标记出来。接下来，agent 可能会去搜索更新的版本，或者改查另一个来源，或者在回答中加入保留说明。系统不再盲目信任相似度得分。

这三种能力，正好分别对应前面那三种失败模式。

Agentic RAG 正是为了解决标准 RAG 在“一次性检索”模式下的短板而设计的。

除了上面这三种能力之外，agentic 系统还可能具备更多能力，比如 memory 和 semantic caching，这样系统就可以在多轮对话中保留上下文。

## 权衡与代价

前面这些内容可能会让 Agentic RAG 看起来像是标准 RAG 的直接升级版。

但循环中的每一次迭代都是有成本的，而这些成本大到足以让很多系统不适合使用它。下面是几个需要考虑的方面：

- **延迟（Latency）**：循环每多一次，就意味着又多一次 LLM 调用、又多一次检索、又多一次评估。一次标准 RAG 查询可能只需要 1～2 秒；而一个经历三到四轮循环的 agentic 查询可能需要 10 秒甚至更久。对于实时聊天应用来说，这通常是不可接受的。
- **成本（Cost）**：agent 的每一次决策都会消耗 token。对于每天处理数千次查询的系统来说，成本可能比标准 RAG 高出 3～10 倍。即使其中 80% 的查询都只是简单 FAQ，这也意味着大量不必要的推理成本。
- **调试与可预测性（Debugging and predictability）**：标准 RAG 相对来说是比较确定的。而 Agentic RAG 会引入更多变化，因为 agent 会根据每一步找到的内容做出不同决策。这会让问题更难复现、更难写测试，也更难向利益相关方解释为什么同一个问题在不同场景下会得到不同答案。
- **评估器悖论（The evaluator paradox）**：自我评估步骤依赖另一次 LLM 调用来判断这次检索是否已经足够好。系统能否自我纠正，取决于这个 LLM 是否善于判断相关性。一个弱的评估器可能会把本来已经很好的结果判为不合格，让系统继续无意义地搜索；也可能接受很差的结果，最后仍然生成错误答案。说到底，我们是在让一个 LLM 去监督另一个 LLM。
- **过度纠正（Overcorrection）**：有时 agentic 循环会比实际所需更“聪明”。它可能会在评估阶段丢弃本来就有用的信息，继续搜索“更好的结果”，最后反而得到一个比第一次结果还差的答案。

这并不意味着不应该使用 Agentic RAG。

它意味着：是否要使用它，应该是一个工程上的决策，而不是默认选择。

对于一个干净、单一来源的知识库中的直接事实查询，并不需要一个推理循环。

对于高并发、低复杂度、延迟和成本比边缘问题处理能力更重要的场景，也同样不需要。

如果一个现有 RAG 系统的大多数问题来自于检索质量本身，例如 chunk 切分不好或者数据过期，那么先修这些问题，带来的收益往往会比叠加一个 agentic 层更大。

理解 Agentic RAG 的核心心智模型其实很简单。

Agentic RAG 把检索从一条一次性的流水线，变成了一个带有决策点的循环。

而这些决策点，就是它全部的价值所在。

在评估或构建 RAG 系统时，下面三个问题有助于抓住本质：

- 系统是否在从正确的来源检索？
- 它能否判断自己检索到的内容是否足够好？
- 如果结果不够好，它是否有能力再试一次？

如果这三个问题的答案都是否定的，而查询本身又很复杂，那么这就是考虑 agentic 方法的信号。

如果查询很简单，知识库也很干净，那么标准 RAG 可能就是正确选择。

## 总结

从“流水线”到“循环”的转变，也并不只发生在 RAG 上。

它反映的是 AI 系统演化中的一个更广泛趋势：从刚性的流水线，走向具备反馈回路和决策能力的系统。

## 参考资料

- [Seven Failure Points When Engineering a Retrieval Augmented Generation System](http://arxiv.org/abs/2401.05856)
- [Agentic Retrieval-Augmented Generation: A Survey on Agentic RAG](http://arxiv.org/abs/2501.09136)

---

> 说明：以下部分不是原文直译，而是基于原文内容做的摘要、归纳和延伸，目的是帮助理解 Agentic RAG 的核心思想与实际应用边界。


这篇文章的核心观点很简单：**标准 RAG 的最大问题，不是“搜不到”，也不是“写不出来”，而是系统不会判断“这次搜回来的东西到底够不够好”。**

标准 RAG 更像一条一次性流水线：用户提问，系统检索，模型回答。如果第一次检索拿回来的内容有歧义、不完整、过期，系统通常也不会停下来检查，而是直接生成答案。

Agentic RAG 的改进点就在这里：它让系统不再只是“检索一次然后回答”，而是变成一个带反馈的循环。系统可以先检索，再评估结果质量，如果发现不够好，就改写查询、切换知识源，或者继续补充检索，再决定是否回答。

从本质上说，Agentic RAG 不是单纯“加个 Agent”这么简单，而是把 RAG 从**一次性命中的流水线**，变成了**可反思、可重试、可决策的闭环系统**。



系统不再默认只查一个知识库，而是会根据问题选择正确的数据源。

这意味着：
- 财务问题可以查数据库
- 政策问题可以查文档库
- 跨领域问题可以同时查多个来源

这一步解决的是“信息根本不在同一个地方”的问题。


Agent 不只是执行用户原始问题，而是会先判断问题是否足够明确。

如果问题模糊，它可以：
- 改写成更具体的问题
- 拆成多个子问题
- 在第一次检索失败后重新表述再试一次

这一步解决的是“问题本身没问清楚”导致的检索偏差。


这是全文最关键的一点。

标准 RAG 会默认：只要检索结果相似度高，就可以交给 LLM 回答。

Agentic RAG 则会进一步问：
- 这些内容真的相关吗？
- 是否足够完整？
- 是否冲突？
- 是否来自过期文档？

也就是说，它不再盲目信任相似度分数，而是引入“检索结果质量判断”这一层。


因为很多真实世界的问题，本来就不是“一次检索就能答对”的。

比如下面这些情况：
- 一个问题可能本身含糊
- 一个答案可能跨多个系统或多份文档
- 最像的问题不一定是最正确的答案
- 文档可能过期，但向量相似度依然很高

标准 RAG 在这些场景里往往会“答得很像，但不一定真对”。

Agentic RAG 更像一个会思考的检索助手：
- 先找
- 再判断
- 不行就重试
- 最后再回答

所以它的提升，不是体现在“LLM 更聪明”，而是体现在**系统流程更像一个真正有判断能力的工作流**。


可以这样理解：

- **标准 RAG**：搜一次，直接答
- **Agentic RAG**：搜一次，先判断，再决定要不要继续搜

再换一种更工程化的表述：

- **标准 RAG**：pipeline（流水线）
- **Agentic RAG**：loop（带反馈的循环）

这也是原文最重要的心智模型。


这篇文章一个很好的地方是，它没有把 Agentic RAG 说成“通用最优解”，而是强调：**这应该是工程决策，而不是默认配置。**

比较适合 Agentic RAG 的场景包括：

例如：
- 企业内部知识问答
- 需要跨政策、流程、数据库联合回答的问题
- 客服、法务、财务等多来源知识系统

如果用户常常提问不完整、上下文不清晰，那么查询改写能力会很重要。

如果系统不能接受“看起来合理但实际上错了”的答案，那么就需要在生成前增加检索质量评估。


原文的这个判断也非常重要。

不适合的典型场景包括：

如果问题都像“退货政策是什么”“工作时间几点开始”这种，标准 RAG 已经足够。

如果你的知识库：
- 干净
- 统一
- 更新及时
- chunk 切分合理

那很多问题并不需要额外的 Agentic 机制。

Agentic RAG 每多一轮：
- 就多一次 LLM 调用
- 多一次检索
- 多一次评估

在高频、低价值、强实时场景下，这个成本很容易不划算。


这篇文章其实不只是讲 RAG，它更像是在讲一个更广泛的 AI 系统设计趋势：

> AI 系统正在从固定流程，逐渐走向带反馈、带判断、能重试的流程。

也就是说，未来很多 AI 系统的优化重点，未必只是“换更强的模型”，而是：
- 增加反馈环
- 增加判断节点
- 增加重试机制
- 增加多工具协同

从这个角度看，Agentic RAG 其实是“Agent 化工作流”在检索增强问答里的一个典型体现。


如果你要把这篇文章的思想应用到自己的项目里，可以按下面这个顺序理解：

先不要急着引入 Agent。

先检查这些基础项：
- 文档是否过期
- chunk 切分是否合理
- embedding 模型是否合适
- 检索召回是否稳定
- 是否存在明显的脏数据

很多所谓“RAG 不行”的问题，根本不是 agent 能解决的，而是数据基础没打好。

再去观察系统到底是怎么失败的：
- 是查错库了？
- 是问题太模糊？
- 是信息分散？
- 是检索到了过期内容？

只有当这些失败模式明确存在时，引入 Agentic RAG 才有意义。

不一定一上来就做多 Agent。

可以先做：
- 一个查询路由器
- 一个检索结果评估器
- 一个“重试一次”的查询改写器

很多时候，这种轻量 Agentic 化已经足够带来明显收益。


如果把这篇文章压缩成一句最值得记住的话，那就是：

> **Agentic RAG 的价值，不在于“更会回答”，而在于“更会判断自己是否该现在回答”。**

这和很多 AI 系统的问题本质是一样的：

- 不是模型不会说
- 而是模型不会在“该停一下再判断”的地方停下来

所以，Agentic RAG 的真正提升，其实是把“决策能力”插入到了“检索与生成之间”。

这一步一旦成立，系统就不再只是一个查询-回答机器，而更像一个真正会做事的知识助手。


如果你以后在做 RAG 或知识库问答系统，可以直接用这三个问题判断是否要上 Agentic RAG：

1. **系统是否经常查错地方？**
2. **系统是否经常检索到“相关但不够用”的内容？**
3. **系统是否需要在回答前做一次质量判断？**

如果这三个问题里，有两个以上经常发生，那么 Agentic RAG 基本就值得考虑。

如果这三个问题都不明显，那大概率应该先继续优化标准 RAG，而不是急着上 Agent。

---

**AI 补充解读（非原文翻译）**

**1. 为什么标准 RAG 容易“答得像对，其实不一定对”**：这篇文章最值得抓住的一点，是它没有把标准 RAG 的问题归结为“检索不行”或“模型不够强”，而是指出了一个更关键的中间层问题：系统不会判断这次检索结果到底够不够好。标准 RAG 往往默认“只要检索到了相似内容，就可以拿去生成答案”，可现实世界的问题远比这个复杂。很多时候，检索结果只是主题相近、措辞相近、语义相近，但并不真正构成正确答案。于是系统就会出现一种典型现象：回答听起来很顺、很合理、也很自信，但真正追问下去会发现它只是在“像回答”，而不是“答对了”。文章强调的，就是这种从检索到生成之间缺乏判断机制的问题。

**2. Agentic RAG 的本质不是“更聪明”，而是“多了一层决策”**：很多人第一次接触 Agentic RAG，会把它理解成“在 RAG 前面套了一个 Agent”，但原文真正想表达的是另一件事：它把原来的一次性流水线，改造成了一个带反馈的循环。标准 RAG 是 query → retrieval → generation，一次走完；而 Agentic RAG 是 retrieval → evaluation → decision → retry or answer。也就是说，它的价值不在于模型突然变聪明了，而在于系统开始具备“是否现在就回答、是否需要继续搜、是否应该换个来源、是否应该重写问题”的能力。你可以把它理解成：标准 RAG 更像一个机械检索员，搜一次就交卷；而 Agentic RAG 更像一个会停下来复核、会补查、会换线索的研究助理。

**3. 文章里最重要的三个能力，实际上对应三类真实失败模式**：原文提到的 Routing、Query Refinement、Self-Evaluation，并不是凭空列出的功能点，而是分别对应真实系统里最常见的三种失败方式。Routing 解决的是“答案不在一个地方”的问题；Query Refinement 解决的是“问题本身不够清楚”的问题；Self-Evaluation 解决的是“检索结果看似相关但其实不够用”的问题。换句话说，Agentic RAG 之所以有意义，不是因为它功能多，而是因为它刚好打在标准 RAG 最痛的几个地方上。这一点对工程实践很重要：如果你的系统里根本没有这些失败模式，那就没必要为了追赶概念去引入一整套 Agentic 机制。

**4. 为什么文章反复强调“不要默认上 Agentic RAG”**：这篇文章其实很克制，它没有把 Agentic RAG说成银弹，而是一直提醒读者，这应该是一个工程选择，而不是时髦默认值。原因很现实：每一次循环、每一次判断、每一次重试，都会带来额外成本。延迟会增加，token 消耗会增加，系统行为会更不稳定，调试难度也会更高。尤其是“评估器悖论”这一点非常关键：如果你让一个 LLM 去判断另一个 LLM 前面的检索是否足够好，本质上你还是在继续信任模型本身的判断力。这个判断环节并不会天然可靠，只是把问题从“生成错答案”转移到了“评估错检索”。所以 Agentic RAG 不是自动更安全，它只是提供了更多自我纠偏的机会，但前提是你的评估环节也足够靠谱。

**5. 在什么场景下，Agentic RAG 才真的值回票价**：如果系统面对的是复杂查询、多来源知识、跨文档整合，而且答案正确性要求比较高，那么 Agentic RAG 才真正有发挥空间。比如企业知识问答、合同与政策交叉检索、财务与法务联合问答、客服场景中的复杂流程解释，这些问题天然就很难靠一次检索命中。相反，如果你面对的是 FAQ、单知识库、低复杂度问答，或者你最主要的问题只是 chunk 切分差、文档过期、召回质量一般，那先优化基础 RAG 往往比上 Agentic RAG 更有效。原文这一点讲得很实在：很多系统不是“不够 agentic”，而是“基础数据工程根本没做好”。

**6. 如果把这篇文章放到实际项目里，该如何真正落地**：更合理的工程路径，不是从“我也要做 Agentic RAG”开始，而是先问：我现在的系统到底是怎么失败的？是查错知识源？是问题太模糊？是答案散落在多个地方？是检索结果看着像对但其实来自旧文档？只有当这些问题明确存在时，引入 Agentic 能力才有意义。而且落地时不一定要一步到位做多 Agent、多轮 ReAct、复杂编排。很多时候，只要先做一个轻量路由器、一个查询改写器、一个结果评估器，就已经比纯标准 RAG 稳定不少。也就是说，Agentic RAG 更像一个渐进增强思路，而不是一套必须完整照搬的大框架。

**7. 最值得记住的一句话**：如果把整篇文章压缩成一句最值得记住的话，那就是：**Agentic RAG 的价值，不在于它“更会回答”，而在于它“更会判断自己是否该现在回答”。** 这其实也是很多 AI 系统未来的共同方向：不是一味追求更大的模型，而是给系统加入反馈环、判断点和重试机制，让它在真正输出之前，多一次思考、多一次检查、多一次纠偏。RAG 只是这个趋势里最容易观察到的一个典型场景。
