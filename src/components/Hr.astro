---
/**
 * 分隔线组件
 *
 * @fileoverview 显示一条水平分隔线
 *
 * 核心逻辑：
 * 1. 响应式宽度：最大宽度 3xl
 * 2. 可选移除内边距（通过 noPadding 属性）
 * 3. 默认对辅助技术隐藏（aria-hidden）
 *
 * 依赖关系：
 * - 被多个页面和组件使用
 * - 用于分隔内容区域
 */

/**
 * 组件属性接口
 *
 * @property noPadding - 是否移除内边距（默认有 padding）
 * @property ariaHidden - 是否对辅助技术隐藏（默认 true）
 */
export interface Props {
  noPadding?: boolean;
  ariaHidden?: boolean;
}

const { noPadding = false, ariaHidden = true } = Astro.props;
---

<div class={`max-w-3xl mx-auto ${noPadding ? "px-0" : "px-0"}`}>
  <hr class="border-border" aria-hidden={ariaHidden} />
</div>
