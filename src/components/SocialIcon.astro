---
/**
 * 社交图标组件
 *
 * @fileoverview 根据 name 渲染对应社交平台图标，使用 src/assets/icons 中的 SVG
 */

import IconGitHub from "@/assets/icons/IconGitHub.svg";
import IconRss from "@/assets/icons/IconRss.svg";
import IconBrandX from "@/assets/icons/IconBrandX.svg";
import IconLinkedin from "@/assets/icons/IconLinkedin.svg";
import IconFacebook from "@/assets/icons/IconFacebook.svg";
import IconMail from "@/assets/icons/IconMail.svg";
import IconTelegram from "@/assets/icons/IconTelegram.svg";
import IconWhatsapp from "@/assets/icons/IconWhatsapp.svg";
import IconPinterest from "@/assets/icons/IconPinterest.svg";

const iconMap: Record<string, typeof IconGitHub> = {
  github: IconGitHub,
  rss: IconRss,
  twitter: IconBrandX,
  x: IconBrandX,
  linkedin: IconLinkedin,
  facebook: IconFacebook,
  mail: IconMail,
  email: IconMail,
  telegram: IconTelegram,
  whatsapp: IconWhatsapp,
  pinterest: IconPinterest,
};

export interface Props {
  /** 图标名称：github | rss | twitter | x | linkedin | facebook | mail | telegram | whatsapp | pinterest */
  name: string;
  width?: number;
  height?: number;
  /** 额外 class（如尺寸、颜色） */
  class?: string;
}

const { name, width = 18, height = 18, class: className = "" } = Astro.props;

const IconComponent = iconMap[name.toLowerCase()];
---

{IconComponent ? (
  <IconComponent
    width={width}
    height={height}
    class={className}
  />
) : null}
